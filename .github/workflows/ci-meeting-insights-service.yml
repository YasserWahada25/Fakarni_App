# CI â€” Meeting Insights Service (Transcriptions + Python IA + MongoDB)
name: CI â€” Meeting Insights Service

on:
  push:
    branches: [main, develop]
    paths:
      - 'backend/meeting-insights-service/**'
      - '.github/workflows/ci-meeting-insights-service.yml'
  pull_request:
    branches: [main]
    paths:
      - 'backend/meeting-insights-service/**'

jobs:
  ci:
    uses: ./.github/workflows/ci-reusable.yml
    with:
      service-name: "Meeting Insights Service"
      service-path: "backend/meeting-insights-service"
      image-name: "fakarni-meeting-insights-service"
      sonar-project-key: "fakarni-meeting-insights-service"
      is-java: true
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
      AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      AZURE_RESOURCE_GROUP: ${{ secrets.AZURE_RESOURCE_GROUP }}
      CONTAINER_APPS_ENV: ${{ secrets.CONTAINER_APPS_ENV }}