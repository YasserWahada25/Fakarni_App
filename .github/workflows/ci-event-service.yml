# CI - Event Service (Mailtrap + Nominatim + MySQL)
name: "CI - Event Service"

on:
  push:
    branches: [main, develop]
    paths:
      - 'backend/Event-Service/**'
      - '.github/workflows/ci-event-service.yml'
      - '.github/workflows/ci-reusable.yml'
  pull_request:
    branches: [main]
    paths:
      - 'backend/Event-Service/**'
  workflow_dispatch:
    inputs:
      reason:
        description: "Raison du declenchement manuel"
        required: false
        default: "Test manuel"

jobs:
  ci:
    uses: ./.github/workflows/ci-reusable.yml
    with:
      service-name: "Event Service"
      service-path: "backend/Event-Service"
      image-name: "fakarni-event-service"
      sonar-project-key: "fakarni-event-service"
      is-java: true
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
      AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      AZURE_RESOURCE_GROUP: ${{ secrets.AZURE_RESOURCE_GROUP }}
      CONTAINER_APPS_ENV: ${{ secrets.CONTAINER_APPS_ENV }}