package com.alzheimer.session_service.entities;

public enum SessionType {
    PRIVATE,  // Séance privée (un docteur et un patient)
    GROUP     // Séance en groupe (plusieurs patients et un docteur)
}
