apiVersion: v1
kind: Service
metadata:
  name: tracking-service
  namespace: fakarni-prod
spec:
  selector:
    app: tracking-service
  ports:
    - port: 9011
      targetPort: 9011
  type: ClusterIP
