apiVersion: v1
kind: Service
metadata:
  name: flask-ia-service
  namespace: fakarni-prod
spec:
  selector:
    app: flask-ia-service
  ports:
    - port: 5000
      targetPort: 5000
  type: ClusterIP
