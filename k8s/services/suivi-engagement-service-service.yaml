apiVersion: v1
kind: Service
metadata:
  name: suivi-engagement-service
  namespace: fakarni-prod
spec:
  selector:
    app: suivi-engagement-service
  ports:
    - port: 8088
      targetPort: 8088
  type: ClusterIP
